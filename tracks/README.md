# Engine tracks

Each track will declare an immutable source digest, ordered patches, build
options, target architectures, toolchain inputs, and required helper binaries.
Do not add a track until a baseline profile needs it.
